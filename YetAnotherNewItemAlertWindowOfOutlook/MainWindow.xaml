<Window x:Class="YetAnotherNewItemAlertWindowOfOutlook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YetAnotherNewItemAlertWindowOfOutlook"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Deactivated="Window_Deactivated">
    <Grid>
        <Menu>
            <MenuItem Header="Refresh">
                <MenuItem Header="Now" Click="RefreshNow_Click"/>
                <MenuItem Header="Pause"  Click="PauseTimer_Click"/>
                <MenuItem Header="Start"  Click="StartTimer_Click"/>
            </MenuItem>
            <!--MenuItem Header="Cache">
                <MenuItem Header="Open cache folder"></MenuItem>
                <MenuItem Header="Clear cache(!)"></MenuItem>
                <MenuItem Header="Delete old cache"></MenuItem>
            </MenuItem-->
            <!--MenuItem Header="Setting">
                <MenuItem Header="Open setting file"></MenuItem>
            </MenuItem-->
        </Menu>
        <DataGrid ItemsSource="{Binding OutlookMailItemCollection}"  AutoGenerateColumns="False" Margin="0,22,0,0" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sender_name" Binding="{Binding SenderName}" IsReadOnly="True"  />
                <DataGridTextColumn Header="Sender_email" Binding="{Binding SenderEmail}"  IsReadOnly="True" />
                <DataGridTextColumn Header="cc" Binding="{Binding Cc}"  IsReadOnly="True" />
                <DataGridTextColumn Header="ReceivedTime" Binding="{Binding ReceivedTime, StringFormat=yyyy/MM/dd HH:mm}"  IsReadOnly="True" />
                <DataGridTextColumn Header="Subject" Binding="{Binding Subject}" IsReadOnly="True"  />
                <DataGridTextColumn Header="Sent_on" Binding="{Binding SentOn, StringFormat=yyyy/MM/dd HH:mm}"  IsReadOnly="True" />
                <DataGridTextColumn Header="categories" Binding="{Binding Categories}"  IsReadOnly="True" />
                <DataGridTextColumn Header="flag" Binding="{Binding FlagIcon}"  IsReadOnly="True"  />
                <DataGridTextColumn Header="recipient_names" Binding="{Binding RecipientNames}"  IsReadOnly="True" />
                <DataGridTextColumn Header="recipient_emails" Binding="{Binding RecipientEmails}"  IsReadOnly="True" />
                <DataGridTextColumn Header="ReminderTime" Binding="{Binding ReminderTime, StringFormat=yyyy/MM/dd HH:mm}" IsReadOnly="True"  />
                <DataGridTextColumn Header="Sent_on" Binding="{Binding SentOn, StringFormat=yyyy/MM/dd HH:mm}"  IsReadOnly="True" />
                <DataGridTextColumn Header="To" Binding="{Binding To}" IsReadOnly="True"  />
                <DataGridTextColumn Header="Unread" Binding="{Binding Unread}" IsReadOnly="True"  />
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                    <EventSetter Event="KeyDown" Handler="DataGridRow_KeyDown"/>
                </Style>
            </DataGrid.RowStyle>

        </DataGrid>


    </Grid>
</Window>
